# Portfolio Analyzer - PostgreSQL Version Requirements
# Production-ready dependencies with PostgreSQL integration

# Core Flask Framework
Flask==2.3.2
Flask-CORS==4.0.0

# PostgreSQL Database
psycopg2-binary==2.9.7         # PostgreSQL adapter for Python
SQLAlchemy==2.0.19              # SQL toolkit and ORM (optional for advanced queries)

# Data Processing & Analysis
pandas==2.0.3                   # Data manipulation and analysis
numpy==1.25.2                   # Numerical computing
yfinance==0.2.18                # Yahoo Finance API
requests==2.31.0                # HTTP library

# Async & Concurrency
aiohttp==3.8.5                  # Async HTTP client/server
asyncio==3.4.3                  # Async programming
concurrent-futures==3.1.1       # Concurrent execution

# Scheduling & Background Jobs
schedule==1.2.0                 # Simple job scheduling
APScheduler==3.10.4             # Advanced Python Scheduler (alternative)

# Authentication & Security
werkzeug==2.3.6                 # WSGI utility library (Flask dependency)
bcrypt==4.0.1                   # Password hashing
PyJWT==2.8.0                    # JSON Web Tokens

# API Rate Limiting & Caching
Flask-Limiter==3.5.0            # Rate limiting for Flask
redis==4.6.0                    # Redis client (optional for advanced caching)

# Monitoring & Logging
python-dotenv==1.0.0            # Environment variable management
structlog==23.1.0               # Structured logging

# Development & Testing (optional)
pytest==7.4.0                   # Testing framework
pytest-asyncio==0.21.1          # Async testing support
pytest-postgresql==5.0.0        # PostgreSQL testing utilities

# Production Server (optional)
gunicorn==21.2.0                # WSGI HTTP Server
uvicorn==0.23.2                 # ASGI server (if using FastAPI features)

# Additional Financial Data Sources
alpha-vantage==2.3.1            # Alpha Vantage API wrapper
finnhub-python==2.4.17          # Finnhub API wrapper

# Data Validation & Serialization
marshmallow==3.20.1             # Object serialization/deserialization
pydantic==2.1.1                 # Data validation using Python type annotations

# Utilities
python-dateutil==2.8.2          # Date/time utilities
pytz==2023.3                    # Timezone definitions
uuid==1.30                      # UUID generation

# Optional: Machine Learning (if keeping ML features)
scikit-learn==1.3.0             # Machine learning library
joblib==1.3.2                   # Lightweight pipelining

# Optional: Advanced Analytics
ta-lib==0.4.28                  # Technical analysis library (requires compilation)
plotly==5.15.0                  # Interactive plotting (for advanced charts)

# Memory & Performance
memory-profiler==0.61.0         # Memory usage profiler
py-spy==0.3.14                  # Production profiler